{
  "name": "amartya-portfolio",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/check-html.mjs",
    "preview": "astro preview",
    "serve": "npm run build && wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy --branch=main",
    "check": "node scripts/check-html.mjs",
    "test": "node scripts/test-ask-guards.mjs",
    "ci": "npm test && npm run build"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.7",
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.4"
  },
  "devDependencies": {
    "wrangler": "^4.124.0"
  }
}
